// Point the console at a hosted backend for production.
// Leave "" to use the same origin (local Flask / docker-compose).
// Example: window.API_BASE = "https://your-api.onrender.com";
window.API_BASE = "";
